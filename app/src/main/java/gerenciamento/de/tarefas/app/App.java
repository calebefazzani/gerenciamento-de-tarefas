/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package gerenciamento.de.tarefas.app;

import gerenciamento.de.tarefas.list.LinkedList;

import static gerenciamento.de.tarefas.utilities.StringUtils.join;
import static gerenciamento.de.tarefas.utilities.StringUtils.split;
import static gerenciamento.de.tarefas.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
